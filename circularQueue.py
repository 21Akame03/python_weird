# array implementation of queue
class Queue() :
    def __init__(self):
        self.front = -1
        self.rear = -1
        arrQueue = []
    
    # if the values are -1 then its empty
    def is_empty(self):
        return self.front == -1 && self.rear == -1
    
    def is_Full(self):
        if self.front == 0 && rear == len(arrQueue):


    # check for emptiness before enqueue
    def enqueue_queue(self):
        # check for overflow
        if self.is_Full():


        if self.is_empty():
            front, rear = 0, 0


